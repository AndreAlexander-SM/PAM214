//Ciclo while

//let count = 0;

/*while(count <= 50){
    console.log(count);
    count++;
}
*/

//Ciclo for
let names = ['ryan', 'joe', 'john'];

for(let i = names.length - 1; i >= 0; i-- ){
    console.log(names[i]);   
}
